---
title: Core UI
description: Buttons and the product card — the base primitives everything else assembles from.
---

# Core UI

Buttons and the product card — the base primitives everything else assembles from.

import BrowserOnly from '@docusaurus/BrowserOnly';
import useBaseUrl from '@docusaurus/useBaseUrl';

export const EmbeddedPreview = () => {
  const src = useBaseUrl('/component-library.html#sec-buttons');
  return (
    <BrowserOnly>
      {() => (
        <iframe
          src={src}
          title="Core UI live preview"
          style={{ width: '100%', height: '600px', border: '1px solid var(--ifm-color-emphasis-300)', borderRadius: '12px', marginBottom: '24px' }}
        />
      )}
    </BrowserOnly>
  );
};

<EmbeddedPreview />

## 1. Button component

Radius switches from 8-10px in Wine/Gold and Day mode to a full pill in Scrollytelling via --btn-radius .

**Preview**

```css
.demo-btn.primary { background: var(--gradient); border-radius: var(--btn-radius); }
```

## 2. Card component, shared-border frame

Badge colour pulls --accent-2 in all three themes.

**Preview**

```html
<div class="demo-card">...</div>
```
